package models

case class Population(
                       id: Option[Int],
                       populationName: String
                       // parentPopulationId: Option[Long]
                     )
